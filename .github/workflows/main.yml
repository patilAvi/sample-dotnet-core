name: Build
on: 
  push:
    branches: [ "feature-*" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:
jobs:  
  build:    
    runs-on: ubuntu-latest
    steps:
      - name: code checkout      
      - uses: actions/checkout@v3
        run: |
            pwd
            ls -ltr


